## `tfm version`

Show the current version and metadata about the `tfm` package.

Usage:

```
$ tfm version
```

Output includes:

- Package version
- Author
- Creation date or release information

Example:

```
$ tfm version
```

